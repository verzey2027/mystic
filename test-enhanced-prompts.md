# คู่มือตรวจสอบ Enhanced AI Prompts

## 🎯 วิธีตรวจสอบแบบง่าย (แนะนำ)

### วิธีที่ 1: ใช้ AI Metrics Dashboard (ง่ายที่สุด)

1. เปิดเว็บ:
```bash
npm run dev
```

2. เปิดหน้า Metrics Dashboard:
```
http://localhost:3000/ai-metrics
```

3. ดูสถิติ:
   - ✅ Enhanced Prompts Status: ต้องเป็น "Active" ทั้ง 4 endpoints
   - 📊 คุณภาพโดยรวม: ควรเป็น "Excellent" หรือ "Good"
   - 📈 Pass Rate: ควรมากกว่า 80%
   - 🔍 Fallback Rate: ควรน้อยกว่า 10%

### วิธีที่ 2: เรียก API Metrics โดยตรง

```bash
# เปิดเว็บก่อน
npm run dev

# เรียก API (ใน terminal อื่น หรือใช้ browser)
curl http://localhost:3000/api/ai/metrics
```

## 📊 วิธีตรวจสอบแบบละเอียด

### 1. ตรวจสอบจากโค้ด ✅

API Routes ทั้ง 4 ตัวใช้ prompt builders แบบใหม่แล้ว:
- ✅ `/api/ai/tarot` → `buildTarotPrompt()`
- ✅ `/api/ai/spirit` → `buildSpiritPrompt()`
- ✅ `/api/ai/numerology` → `buildNumerologyPrompt()`
- ✅ `/api/ai/tarot-chat` → `buildChatPrompt()`

### 2. ตรวจสอบจากคำตอบจริง (Manual Testing)

ทดสอบแต่ละ feature และดูว่าคำตอบมีลักษณะตามนี้หรือไม่:

#### 🔮 Tarot Reading
**สิ่งที่ต้องมี:**
- [ ] มีการอ้างอิงหลักธรรมพุทธ (กรรม, บุญ, สติ)
- [ ] คำแนะนำเฉพาะเจาะจง ไม่คลุมเครือ
- [ ] มีขั้นตอนการทำ (1-2 สัปดาห์, สัปดาห์ที่ 3-4)
- [ ] อธิบายความสัมพันธ์ระหว่างไพ่ (สำหรับ 3 หรือ 10 ใบ)
- [ ] เน้นความสำคัญของ Major Arcana (ถ้ามี)
- [ ] อธิบาย shadow aspect สำหรับไพ่กลับหัว

**ทดสอบ:**
```bash
# เปิดเว็บและทดสอบ
npm run dev
# ไปที่ http://localhost:3000/tarot
# เลือกไพ่และดูคำตอบ
```

#### 🌟 Spirit Card
**สิ่งที่ต้องมี:**
- [ ] อธิบายความเชื่อมโยงระหว่างเลขเส้นทางชีวิตกับไพ่
- [ ] บอกจุดแข็งธรรมชาติ (ถ้าไพ่ตั้งตรง)
- [ ] บอกจุดที่ต้องพัฒนา (ถ้าไพ่กลับหัว)
- [ ] คำแนะนำระยะยาว ไม่ใช่แค่ช่วงสั้น
- [ ] เชื่อมโยงกับการพัฒนาตนเอง

**ทดสอบ:**
```bash
# ไปที่ http://localhost:3000/spirit-card
# ใส่วันเกิดและดูคำตอบ
```

#### 🔢 Numerology
**สิ่งที่ต้องมี:**
- [ ] อธิบายความหมายของเลขราก
- [ ] ใช้น้ำเสียงเหมาะสมกับคะแนน (บวกสำหรับคะแนนสูง, สร้างสรรค์สำหรับคะแนนต่ำ)
- [ ] วิเคราะห์แต่ละด้าน (งาน, เงิน, ความสัมพันธ์) พร้อมตัวอย่าง
- [ ] อ้างอิงความเชื่อไทยเรื่องตัวเลข (เลข 9 มงคล, เลข 8 มั่งคั่ง)

**ทดสอบ:**
```bash
# ไปที่ http://localhost:3000/numerology
# ใส่เบอร์โทรและดูคำตอบ
```

#### 💬 Tarot Chat
**สิ่งที่ต้องมี:**
- [ ] อ้างอิงไพ่จากการดูดวงเดิม
- [ ] คำตอบกระชับ (1-3 ย่อหน้า)
- [ ] มีความเห็นอกเห็นใจ
- [ ] เน้นสิ่งที่ผู้ใช้ควบคุมได้

**ทดสอบ:**
```bash
# หลังจากดูไพ่ทาโรต์แล้ว
# ลองถามคำถามเพิ่มเติม
```

### 3. ตรวจสอบจาก Validation

ดูว่า validation ทำงานหรือไม่:

**ตรวจสอบ validation module:**
```bash
# ดูว่ามี validation.ts หรือไม่
ls src/lib/ai/validation.ts
```

**สิ่งที่ validation ต้องตรวจ:**
- [ ] Summary มีอย่างน้อย 50 ตัวอักษรภาษาไทย
- [ ] cardStructure มีครบ 3 ส่วน:
  - ภาพรวมสถานการณ์
  - จุดที่ควรระวัง
  - แนวทางที่ควรทำ

### 4. ตรวจสอบจาก Console Logs

เปิด Browser DevTools และดู:
- [ ] ไม่มี validation errors
- [ ] ไม่มี fallback ถูกใช้บ่อยเกินไป
- [ ] Response time ไม่เกิน 5 วินาที

### 5. เปรียบเทียบก่อน-หลัง

**คำตอบแบบเก่า (ก่อน enhanced prompts):**
- คลุมเครือ เช่น "คุณจะประสบความสำเร็จ"
- ไม่มีขั้นตอนชัดเจน
- ไม่มีบริบททางวัฒนธรรมไทย

**คำตอบแบบใหม่ (หลัง enhanced prompts):**
- เฉพาะเจาะจง เช่น "ภายใน 1-2 สัปดาห์: รวบรวมข้อมูล..."
- มีขั้นตอนชัดเจนพร้อมกรอบเวลา
- มีการอ้างอิงหลักธรรมพุทธ, โหราศาสตร์ไทย

## สรุป

หากคำตอบมีลักษณะตามที่ระบุข้างต้น แสดงว่า AI ใช้ prompt แบบใหม่แล้ว ✅

หากยังไม่มี ให้ตรวจสอบ:
1. API routes import prompt builders ถูกต้องหรือไม่
2. Gemini API key ใช้งานได้หรือไม่
3. มี error ใน console หรือไม่


---

## 🚀 Quick Start

**วิธีที่เร็วที่สุด:**

1. เปิดเว็บ:
```bash
npm run dev
```

2. เปิด Metrics Dashboard:
```
http://localhost:3000/ai-metrics
```

3. ทดสอบแต่ละ feature:
   - Tarot: http://localhost:3000/tarot
   - Spirit Card: http://localhost:3000/spirit-card
   - Numerology: http://localhost:3000/numerology

4. กลับมาดู Metrics Dashboard อีกครั้ง:
   - ดูว่า Pass Rate เพิ่มขึ้น
   - ตรวจสอบว่าไม่มี errors
   - ดูว่า Fallback Rate ต่ำ

---

## 📈 ตัวชี้วัดความสำเร็จ

### ✅ Enhanced Prompts ทำงานได้ดี ถ้า:

1. **Pass Rate ≥ 80%**
   - คำตอบส่วนใหญ่ผ่านการตรวจสอบ

2. **Fallback Rate < 10%**
   - ไม่ต้องใช้ fallback บ่อย

3. **คำตอบมีลักษณะตามที่กำหนด:**
   - มีบริบททางวัฒนธรรมไทย
   - คำแนะนำเฉพาะเจาะจง
   - มีขั้นตอนชัดเจน

4. **ไม่มี errors ซ้ำๆ**
   - ไม่มี validation errors เยอะ

### ⚠️ ต้องปรับปรุง ถ้า:

1. **Pass Rate < 80%**
   - หลายคำตอบไม่ผ่านการตรวจสอบ
   - ต้องตรวจสอบ prompts

2. **Fallback Rate > 10%**
   - ใช้ fallback บ่อยเกินไป
   - prompts อาจไม่ชัดเจนพอ

3. **มี errors ซ้ำๆ**
   - summary_too_short: prompt ไม่ได้บอกให้ตอบยาวพอ
   - cardstructure_incomplete_sections: prompt ไม่ได้บอกให้ใส่ครบ 3 ส่วน

---

## 🔧 Troubleshooting

### ปัญหา: Metrics Dashboard แสดง "No data"

**วิธีแก้:**
1. ทดสอบแต่ละ feature ก่อน (Tarot, Spirit, Numerology)
2. รอให้ AI ตอบกลับมา
3. Refresh Metrics Dashboard

### ปัญหา: Pass Rate ต่ำ

**วิธีแก้:**
1. ดู Recent Errors ใน Metrics Dashboard
2. ตรวจสอบว่า error type อะไรเกิดบ่อย
3. ปรับ prompts ตาม error type:
   - `summary_too_short`: เพิ่มคำสั่งให้ตอบยาวขึ้น
   - `cardstructure_incomplete_sections`: เน้นย้ำให้ใส่ครบ 3 ส่วน

### ปัญหา: Fallback Rate สูง

**วิธีแก้:**
1. ตรวจสอบว่า Gemini API key ใช้งานได้
2. ดู console logs ว่ามี API errors หรือไม่
3. ตรวจสอบ prompts ว่าชัดเจนพอหรือไม่

---

## 📝 Checklist สำหรับการตรวจสอบ

### ก่อนเริ่มใช้งาน:
- [ ] เปิดเว็บได้ (`npm run dev`)
- [ ] Gemini API key ตั้งค่าแล้ว (`.env.local`)
- [ ] เข้า Metrics Dashboard ได้ (`/ai-metrics`)

### หลังทดสอบแต่ละ feature:
- [ ] Tarot: คำตอบมีขั้นตอนชัดเจน
- [ ] Spirit Card: เชื่อมโยงเลขเส้นทางชีวิต
- [ ] Numerology: อธิบายเลขราก
- [ ] Chat: อ้างอิงไพ่จากการดูดวงเดิม

### ตรวจสอบ Metrics:
- [ ] Pass Rate ≥ 80%
- [ ] Fallback Rate < 10%
- [ ] ไม่มี errors ซ้ำๆ
- [ ] Enhanced Prompts Status: All Active

---

## 💡 Tips

1. **ทดสอบบ่อยๆ**: ยิ่งทดสอบมาก metrics ยิ่งแม่นยำ
2. **ดู Recent Errors**: จะบอกว่าปัญหาอยู่ตรงไหน
3. **เปรียบเทียบ**: ลองดูคำตอบก่อน-หลัง enhanced prompts
4. **Refresh Dashboard**: กด Refresh เพื่อดูข้อมูลล่าสุด

---

## 🎓 สรุป

**Enhanced Prompts ทำงานแล้ว ✅** ถ้า:
- Metrics Dashboard แสดงสถานะ "Active" ทั้งหมด
- Pass Rate สูง (≥ 80%)
- คำตอบมีคุณภาพตามที่กำหนด

**ต้องตรวจสอบเพิ่ม ⚠️** ถ้า:
- Pass Rate ต่ำ (< 80%)
- Fallback Rate สูง (> 10%)
- มี errors เยอะ

**ติดต่อ Developer 🔧** ถ้า:
- Metrics Dashboard ไม่ทำงาน
- API errors ซ้ำๆ
- คำตอบผิดพลาดทั้งหมด
